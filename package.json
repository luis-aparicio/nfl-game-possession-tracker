{
  "devDependencies": {
    "dotenv": "^17.2.3",
    "esbuild": "^0.20.0",
    "express": "^5.2.1",
    "http-proxy-middleware": "^3.0.5",
    "purescript": "^0.15.15",
    "purs-backend-es": "^1.4.3",
    "spago": "^0.21.0"
  },
  "scripts": {
    "postinstall": "spago install",
    "clean": "rm -rf node_modules output .spago dist/*.js *.lock .cache",
    "build": "spago build",
    "test": "spago test",
    "bundle": "spago bundle-app --main Main --to dist/index.js",
    "proxy": "node scripts/proxy-server.js",
    "serve": "npm run bundle && npm run proxy"
  }
}
